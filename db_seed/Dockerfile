FROM pkosiec/mongo-seeding:latest

COPY ./seed /import-data

ENV DB_HOST $DB_HOST
ENV DB_NAME $DB_NAME
ENV DB_PORT $DB_PORT
ENV DROP_DATABASE false
#$DROP_DATABASE
ENV REPLACE_ID $REPLACE_ID
ENV DEBUG=mongo-seeding
ENV RECONNECT_TIMEOUT=10000

WORKDIR /import-data